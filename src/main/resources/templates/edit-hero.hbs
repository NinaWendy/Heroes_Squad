<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hero_Squad</title>
    <link rel="shortcut icon" href="./images/shapes-outline.svg" type="image/x-icon">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
          integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/index.css">
</head>

<body>

{{#if editHero}}
    <div class="heroForm">
        <form action="/hero/{{editHero.id}}/update" method="post" style="width: 30%; margin-left: 35%;">            <span class="badge bg-warning"><a href="/">Home</a> </span>
            <span class="badge bg-primary"><a href="/hero">Heroes</a></span>
            <span class="badge bg-success"><a href="./squad">Squad</a> </span>
            <span class="badge bg-danger">Battle</span>
            <h2>EDIT HERO</h2>
            <div class="mb-3 mt-3">
                <label for="name" class="form-label">Hero Name:</label>
                <input type="text" class="form-control" id="name" value="{{editHero.name}}" name="name">
            </div>
            <div class="mb-3 mt-3">
                <label for="age" class="form-label">Hero Age:</label>
                <input type="text" class="form-control" id="age" value="{{editHero.age}}" name="age">
            </div>
            <div class="mb-3">
                <div class="form-group">
                    <label for="squad">Select squad:</label>
                    <select class="form-control" id="squad" name="squad">
                        <option>X-Men</option>
                        <option>Marvel</option>
                        <option>DC</option>
                        <option>Avengers</option>
                    </select>
                </div>
            </div>
            <div class="mb-3">
                <label for="strength" class="form-label">Strength:</label>
                <input type="text" class="form-control" id="strength" value="{{editHero.strength}}" name="strength">
            </div>
            <div class="mb-3">
                <label for="weakness" class="form-label">Weakness:</label>
                <input type="text" class="form-control" id="weakness" value="{{editHero.weakness}}" name="weakness">
            </div>
            <div class="mb-3">
                <label for="description">Description:</label>
                <textarea class="form-control" rows="2" id="description" name="description" >
                    {{editHero.description}}
                </textarea>
            </div>

            <button type="submit" class="btn btn-primary">Submit</button>
        </form>
{{/if}}
</div>
</body>
</html>